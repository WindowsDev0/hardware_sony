allow system_app proc_pagetypeinfo:file { read open getattr };

allow system_app hal_health_default:binder call;
allow system_app init:binder call;
allow system_app hal_nfc_default:binder call;
allow system_app hal_power_default:binder call;
allow system_app hal_wifi_default:binder call;
allow system_app hal_sensors_default:binder call;

allow system_app system_suspend_control_service:service_manager find;
allow system_app system_suspend_control_internal_service:service_manager find;

allow system_app default_android_service:service_manager find;

allow system_app proc_pagetypeinfo:file { read open getattr };

allow system_app sysfs:file { read open getattr write };

allow system_app vendor_hal_disp_hwservice:hwservice_manager find;

allow system_app hal_miscta_service:service_manager find;

allow system_app vendor_hal_disp_default:binder { call transfer };

allow system_app sysfs_kprofiles:file { open read getattr };
allow system_app sysfs_kprofiles:dir { open getattr };

allow system_app sysfs_tsp:file { open getattr };

# Allow KProfiles to be adjusted by a system app
allow system_app sysfs_kprofiles:file rw_file_perms;
